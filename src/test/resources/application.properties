spring.datasource.url=jdbc:postgresql://localhost:5432/gimnasio_test
spring.datasource.username=gimnasio_app
spring.datasource.password=gimnasio_local_dev
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false

spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1

jwt.secret=mHmmNagV2AyEtN/cQXlvGQnrZRJsfdjhdVRlJhYOS8k=
jwt.access-expiration-ms=1800000
jwt.refresh-expiration-ms=2592000000

app.cookie.secure=false
app.cookie.same-site=Lax

app.cors.allowed-origins=http://localhost:5173

# Alto a propósito: la suite de integración hace muchos logins reales contra el mismo
# RateLimitFilter (bean singleton, compartido por todos los tests de la clase/contexto).
# El límite real (5/min) se prueba aparte, en un test unitario aislado del filtro.
app.security.rate-limit.capacidad=10000
